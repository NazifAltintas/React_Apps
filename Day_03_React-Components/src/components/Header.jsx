import React from "react";

function Header() {
  return <h1>Hello World</h1>;
}

// by default this compenents is private. since export it will be visiable from the other folder
export default Header;
